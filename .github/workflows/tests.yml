# Tests — DEPRECATED
#
# test-unit (Jest) e test-e2e (Playwright) foram removidos porque:
#   1. Jest sem config de transform (sem babel/jest.config) — 29 suites falham por parsing
#   2. E2E precisa de standalone server que não é gerado pelo build atual
#   3. Type-check (tsc) e build já são cobertos pelo ci.yml
#
# Reativar quando Jest tiver config adequada e E2E tiver server configurado.
#
# Histórico:
#   - Removido em 2026-06-02 — ambos os jobs falhavam consistentemente
#   - CI pipeline funcional em .github/workflows/ci.yml

name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # test-unit e test-e2e removidos — ver ci.yml para type-check e build
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Tests workflow desativado. Usar ci.yml para verificação."
